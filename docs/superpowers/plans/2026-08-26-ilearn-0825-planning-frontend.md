# Edition 0825 Planning + Frontend Plan

> Do not commit docs/. Commit code only if user asks.

**Goal:** Land scientific planning enrichment + useRole/useResponsive.

## Tasks
1. DiagnosisAgent enrichment + tests
2. PlanningAgent scientific_plan + markdown + tests
3. TutorAgent strategy prefix + orchestrator wire + tests
4. Frontend hooks + App/CSS + tests
5. VERSION.md note + verify pytest/frontend tests

**Constraint:** PlanDay unchanged; assessment 20 items unchanged.
